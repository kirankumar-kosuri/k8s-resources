apiVersion: v1
kind: Pod
metadata:
  name: service-np-demo
  labels:
    project: roboshop
    environment: dev
    component: frontend
    purpose: service-np
spec:
  containers:
  - name: nginx
    image: nginx