apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    environment: prod
    component: frontend
    version: v1
    purpose: load-balancer

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30007