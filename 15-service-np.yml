apiVersion: v1
kind: Service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector:
    project: roboshop
    environment: prod
    component: frontend
    version: v1
    purpose: nginx-np
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80