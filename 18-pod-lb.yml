apiVersion: v1
kind: Pod
metadata:
  name: service-lb-demo
  labels:
    project: roboshop
    environment: prod
    component: frontend
    version: v1
    purpose: load-balancer
spec:
  containers:
  - name: nginx
    image: nginx