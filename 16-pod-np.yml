apiVersion: v1
kind: Pod
metadata:
  name: service-np-demo
  labels:
    project: roboshop
    environment: prod
    component: frontend
    version: v1
    purpose: nginx-np
spec:
  containers:
  - name: nginx
    image: nginx