apiVersion: v1
kind: Pod
metadata:
  name: resources-demo
  labels:
    project: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
      - containerPort: 80
    resources:
      requests:
        memory: "68Mi"
        cpu: "100m"
      limits:
        memory: "100Mi"
        cpu: "120m"