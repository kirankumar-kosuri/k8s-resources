apiVersion: v1
kind: Pod

metadata:
  name: resources

  labels:
    purpose: resources-utilization

spec:
  containers:
  - name: nginx
    image: nginx

    resources:
      requests:  # soft 
        memory: "68Mi"
        cpu: "100m"

      limits:  # hard
        memory: "100Mi"
        cpu: "120m"