apiVersion: v1
kind: Service
metadata:
  name: service-demo
spec:
  selector:
    project: roboshop
    environment: prod
    component: frontend
    version: v1
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80