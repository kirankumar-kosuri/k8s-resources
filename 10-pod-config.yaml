apiVersion: v1
kind: Pod
metadata:
  name: configmap-demo
  labels:
    project: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: pod-config